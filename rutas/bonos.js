/**
 * rutas/bonos.js — ELIMINADO
 * El sistema de bonos/abonos/créditos fue reemplazado por inscripción directa
 * vía SOCIO_TURNO con validación de capacidad_maxima.
 *
 * Este archivo se conserva vacío para no romper el import en server.js
 * hasta que se elimine la referencia ahí también.
 */
import express from 'express'
const router = express.Router()
export default router
